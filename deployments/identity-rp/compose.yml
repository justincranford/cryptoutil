# $schema: https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json
#
# IDENTITY-RP Docker Compose Configuration
#
# SERVICE-level deployment for Relying Party.
# Supports 4 instances: 2 SQLite + 2 PostgreSQL.
#
# Port allocation (SERVICE level: 8700-8799):
#   - identity-rp-app-sqlite-1:      8700
#   - identity-rp-app-sqlite-2:      8701
#   - identity-rp-app-postgresql-1:  8702
#   - identity-rp-app-postgresql-2:  8703
#
# Dual Role: Standalone deployable AND include target for PRODUCT/SUITE compose files.
# Usage:
#   docker compose -f compose.yml up -d                            # All instances
#   docker compose -f compose.yml up identity-rp-app-sqlite-1 -d   # Single instance
#   docker compose -f compose.yml logs -f                          # View logs
#   docker compose -f compose.yml down -v                          # Stop and clean up
#
# Health Checks:
#   - Public health endpoint: https://localhost:8700/browser/api/v1/health
#   - Admin livez endpoint: https://127.0.0.1:9090/admin/api/v1/livez (internal only)
#
include:
  - path: ../shared-telemetry/compose.yml
  - path: ../shared-postgres/compose.yml

services:
  # Healthcheck for Docker secrets availability.
  healthcheck-secrets:
    image: alpine:latest
    secrets:
      - unseal-1of5.secret
      - unseal-2of5.secret
      - unseal-3of5.secret
      - unseal-4of5.secret
      - unseal-5of5.secret
      - hash-pepper-v3.secret
      - postgres-url.secret
      - postgres-username.secret
      - postgres-password.secret
      - postgres-database.secret
      - browser-username.secret
      - browser-password.secret
      - service-username.secret
      - service-password.secret
    command:
      - sh
      - -c
      - |
        test -f /run/secrets/unseal-1of5.secret &&
        test -f /run/secrets/unseal-2of5.secret &&
        test -f /run/secrets/unseal-3of5.secret &&
        test -f /run/secrets/unseal-4of5.secret &&
        test -f /run/secrets/unseal-5of5.secret &&
        test -f /run/secrets/hash-pepper-v3.secret &&
        test -f /run/secrets/postgres-url.secret &&
        test -f /run/secrets/postgres-username.secret &&
        test -f /run/secrets/postgres-password.secret &&
        test -f /run/secrets/postgres-database.secret &&
        test -f /run/secrets/browser-username.secret &&
        test -f /run/secrets/browser-password.secret &&
        test -f /run/secrets/service-username.secret &&
        test -f /run/secrets/service-password.secret &&
        echo All secrets verified
    networks:
      - identity-rp-network

  # Build identity-rp image from source.
  builder-identity-rp:
    image: cryptoutil-identity-rp:local
    build:
      context: ../..
      dockerfile: deployments/identity-rp/Dockerfile
      args:
        APP_VERSION: "local"
        VCS_REF: "local"
        BUILD_DATE: "2026-02-17T00:00:00Z"
    entrypoint: ["sh", "-c"]
    command: ["echo 'Build completed successfully'"]
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully

  # Thorough Go binary secrets validation: verifies file presence, naming convention,
  # location (/run/secrets/), content length (>=43 chars), and format (base64url).
  # Runs after builder-identity-rp so the binary is available, before pki-init.
  validate-secrets-identity-rp:
    image: cryptoutil-identity-rp:local
    command: ["/app/identity-rp", "validate-secrets"]
    secrets:
      - unseal-1of5.secret
      - unseal-2of5.secret
      - unseal-3of5.secret
      - unseal-4of5.secret
      - unseal-5of5.secret
      - hash-pepper-v3.secret
      - postgres-url.secret
      - postgres-username.secret
      - postgres-password.secret
      - postgres-database.secret
      - browser-username.secret
      - browser-password.secret
      - service-username.secret
      - service-password.secret
    depends_on:
      builder-identity-rp:
        condition: service_completed_successfully
    networks:
      - identity-rp-network

  # Init job: bootstraps service prerequisites before app startup.
  pki-init:
    image: cryptoutil-identity-rp:local
    command: ["/app/identity-rp", "init", "--domain=identity-rp", "--output-dir=/certs"]
    volumes:
      - identity-rp-certs:/certs:rw
    depends_on:
      builder-identity-rp:
        condition: service_completed_successfully
      validate-secrets-identity-rp:
        condition: service_completed_successfully
    networks:
      - identity-rp-network

  # identity-rp server with SQLite backend (instance 1).
  identity-rp-app-sqlite-1:
    image: cryptoutil-identity-rp:local
    command: /bin/sh -c "exec /app/identity-rp server --config=/certs/tls-config.yml --config=/app/config/identity-rp-app-framework-common.yml --config=/app/config/identity-rp-app-framework-sqlite-1.yml --config=/app/config/identity-rp-app-domain-common.yml --config=/app/config/identity-rp-app-domain-sqlite-1.yml --config=/app/otel/otel.yml --bind-public-port=8080 -u sqlite://file::memory:?cache=shared $$SUITE_ARGS"
    working_dir: /tmp
    ports:
      - "8700:8080" # HTTPS public
    volumes:
      - ./config/identity-rp-app-framework-common.yml:/app/config/identity-rp-app-framework-common.yml:ro
      - ./config/identity-rp-app-framework-sqlite-1.yml:/app/config/identity-rp-app-framework-sqlite-1.yml:ro
      - ./config/identity-rp-app-domain-common.yml:/app/config/identity-rp-app-domain-common.yml:ro
      - ./config/identity-rp-app-domain-sqlite-1.yml:/app/config/identity-rp-app-domain-sqlite-1.yml:ro
      - ../shared-telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
      - identity-rp-certs:/certs:ro
    secrets:
      - unseal-1of5.secret
      - unseal-2of5.secret
      - unseal-3of5.secret
      - unseal-4of5.secret
      - unseal-5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-identity-rp:
        condition: service_completed_successfully
      validate-secrets-identity-rp:
        condition: service_completed_successfully
      pki-init:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
      healthcheck-opentelemetry-collector-contrib:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "/app/identity-rp", "livez", "--cacert", "/certs/root-ca.pem"]
      start_period: 30s
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - identity-rp-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # identity-rp server with SQLite backend (instance 2).
  identity-rp-app-sqlite-2:
    image: cryptoutil-identity-rp:local
    command: /bin/sh -c "exec /app/identity-rp server --config=/certs/tls-config.yml --config=/app/config/identity-rp-app-framework-common.yml --config=/app/config/identity-rp-app-framework-sqlite-2.yml --config=/app/config/identity-rp-app-domain-common.yml --config=/app/config/identity-rp-app-domain-sqlite-2.yml --config=/app/otel/otel.yml --bind-public-port=8080 -u sqlite://file::memory:?cache=shared $$SUITE_ARGS"
    working_dir: /tmp
    ports:
      - "8701:8080" # HTTPS public
    volumes:
      - ./config/identity-rp-app-framework-common.yml:/app/config/identity-rp-app-framework-common.yml:ro
      - ./config/identity-rp-app-framework-sqlite-2.yml:/app/config/identity-rp-app-framework-sqlite-2.yml:ro
      - ./config/identity-rp-app-domain-common.yml:/app/config/identity-rp-app-domain-common.yml:ro
      - ./config/identity-rp-app-domain-sqlite-2.yml:/app/config/identity-rp-app-domain-sqlite-2.yml:ro
      - ../shared-telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
      - identity-rp-certs:/certs:ro
    secrets:
      - unseal-1of5.secret
      - unseal-2of5.secret
      - unseal-3of5.secret
      - unseal-4of5.secret
      - unseal-5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-identity-rp:
        condition: service_completed_successfully
      validate-secrets-identity-rp:
        condition: service_completed_successfully
      pki-init:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
      healthcheck-opentelemetry-collector-contrib:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "/app/identity-rp", "livez", "--cacert", "/certs/root-ca.pem"]
      start_period: 30s
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - identity-rp-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # identity-rp server with PostgreSQL backend (instance 1).
  identity-rp-app-postgresql-1:
    image: cryptoutil-identity-rp:local
    command: /bin/sh -c "exec /app/identity-rp server --config=/certs/tls-config.yml --config=/app/config/identity-rp-app-framework-common.yml --config=/app/config/identity-rp-app-framework-postgresql-1.yml --config=/app/config/identity-rp-app-domain-common.yml --config=/app/config/identity-rp-app-domain-postgresql-1.yml --config=/app/otel/otel.yml --bind-public-port=8080 -u file:///run/secrets/postgres-url.secret $$SUITE_ARGS"
    working_dir: /tmp
    ports:
      - "8702:8080" # HTTPS public
    volumes:
      - ./config/identity-rp-app-framework-common.yml:/app/config/identity-rp-app-framework-common.yml:ro
      - ./config/identity-rp-app-framework-postgresql-1.yml:/app/config/identity-rp-app-framework-postgresql-1.yml:ro
      - ./config/identity-rp-app-domain-common.yml:/app/config/identity-rp-app-domain-common.yml:ro
      - ./config/identity-rp-app-domain-postgresql-1.yml:/app/config/identity-rp-app-domain-postgresql-1.yml:ro
      - ../shared-telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
      - identity-rp-certs:/certs:ro
    secrets:
      - unseal-1of5.secret
      - unseal-2of5.secret
      - unseal-3of5.secret
      - unseal-4of5.secret
      - unseal-5of5.secret
      - postgres-url.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-identity-rp:
        condition: service_completed_successfully
      validate-secrets-identity-rp:
        condition: service_completed_successfully
      pki-init:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
      healthcheck-opentelemetry-collector-contrib:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "/app/identity-rp", "livez", "--cacert", "/certs/root-ca.pem"]
      start_period: 30s
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - identity-rp-network
      - telemetry-network
      - postgres-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # identity-rp server with PostgreSQL backend (instance 2 - shared DB with instance 1).
  identity-rp-app-postgresql-2:
    image: cryptoutil-identity-rp:local
    command: /bin/sh -c "exec /app/identity-rp server --config=/certs/tls-config.yml --config=/app/config/identity-rp-app-framework-common.yml --config=/app/config/identity-rp-app-framework-postgresql-2.yml --config=/app/config/identity-rp-app-domain-common.yml --config=/app/config/identity-rp-app-domain-postgresql-2.yml --config=/app/otel/otel.yml --bind-public-port=8080 -u file:///run/secrets/postgres-url.secret $$SUITE_ARGS"
    working_dir: /tmp
    ports:
      - "8703:8080" # HTTPS public
    volumes:
      - ./config/identity-rp-app-framework-common.yml:/app/config/identity-rp-app-framework-common.yml:ro
      - ./config/identity-rp-app-framework-postgresql-2.yml:/app/config/identity-rp-app-framework-postgresql-2.yml:ro
      - ./config/identity-rp-app-domain-common.yml:/app/config/identity-rp-app-domain-common.yml:ro
      - ./config/identity-rp-app-domain-postgresql-2.yml:/app/config/identity-rp-app-domain-postgresql-2.yml:ro
      - ../shared-telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
      - identity-rp-certs:/certs:ro
    secrets:
      - unseal-1of5.secret
      - unseal-2of5.secret
      - unseal-3of5.secret
      - unseal-4of5.secret
      - unseal-5of5.secret
      - postgres-url.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-identity-rp:
        condition: service_completed_successfully
      validate-secrets-identity-rp:
        condition: service_completed_successfully
      pki-init:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
      healthcheck-opentelemetry-collector-contrib:
        condition: service_completed_successfully
      identity-rp-app-postgresql-1:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "/app/identity-rp", "livez", "--cacert", "/certs/root-ca.pem"]
      start_period: 30s
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - identity-rp-network
      - telemetry-network
      - postgres-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

volumes:
  identity-rp-certs:

networks:
  identity-rp-network:
    driver: bridge

secrets:
  unseal-1of5.secret:
    file: ./secrets/unseal-1of5.secret # pragma: allowlist secret
  unseal-2of5.secret:
    file: ./secrets/unseal-2of5.secret
  unseal-3of5.secret:
    file: ./secrets/unseal-3of5.secret
  unseal-4of5.secret:
    file: ./secrets/unseal-4of5.secret
  unseal-5of5.secret:
    file: ./secrets/unseal-5of5.secret
  hash-pepper-v3.secret:
    file: ./secrets/hash-pepper-v3.secret
  postgres-url.secret:
    file: ./secrets/postgres-url.secret
  postgres-username.secret:
    file: ./secrets/postgres-username.secret
  postgres-password.secret:
    file: ./secrets/postgres-password.secret
  postgres-database.secret:
    file: ./secrets/postgres-database.secret
  browser-username.secret:
    file: ./secrets/browser-username.secret
  browser-password.secret:
    file: ./secrets/browser-password.secret
  service-username.secret:
    file: ./secrets/service-username.secret
  service-password.secret:
    file: ./secrets/service-password.secret
