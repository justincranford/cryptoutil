# $schema: https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json
#
# CRYPTOUTIL Suite Docker Compose Configuration
#
# This is the COMPLETE SUITE deployment with ALL 9 services across 5 products.
# Each service has 3 instances (1 SQLite + 2 PostgreSQL).
#
# Services (9 total):
#   1. pki-ca (PKI Certificate Authority)
#   2. jose-ja (JOSE JWK Authority)
#   3. cipher-im (Cipher Instant Messenger)
#   4. sm-kms (Secret Manager Key Management Service)
#   5. identity-authz (Identity Authorization Server)
#   6. identity-idp (Identity Provider)
#   7. identity-rs (Resource Server)
#   8. identity-rp (Relying Party)
#   9. identity-spa (Single Page App)
#
# Port allocation:
#   - pki-ca:         8050 (SQLite), 8051-8052 (PostgreSQL)
#   - jose-ja:        8060 (SQLite), 8061-8062 (PostgreSQL)
#   - cipher-im:      8070 (SQLite), 8071-8072 (PostgreSQL)
#   - sm-kms:         8080 (SQLite), 8081-8082 (PostgreSQL)
#   - identity-authz: 8100 (SQLite), 8101-8102 (PostgreSQL)
#   - identity-idp:   8110 (SQLite), 8111-8112 (PostgreSQL)
#   - identity-rs:    8120 (SQLite), 8121-8122 (PostgreSQL)
#   - identity-rp:    8130 (SQLite), 8131-8132 (PostgreSQL)
#   - identity-spa:   8140 (SQLite), 8141-8142 (PostgreSQL)
#
# Database ports (PostgreSQL leader/follower):
#   - postgres-leader:      5432
#   - postgres-follower:    5433
#   - citus-coordinator:    5434
#   - citus-worker-1:       5435
#   - citus-worker-2:       5436
#
# Usage:
#   docker compose -f compose-cryptoutil.yml up -d                        # SQLite instances only (default: dev profile)
#   docker compose -f compose-cryptoutil.yml --profile postgres up -d     # All instances (SQLite + 2x PostgreSQL per service)
#   docker compose -f compose-cryptoutil.yml --profile demo up -d         # Demo mode with seed data
#   docker compose -f compose-cryptoutil.yml --profile ci up -d           # CI mode for testing
#   docker compose -f compose-cryptoutil.yml logs -f                      # View logs
#   docker compose -f compose-cryptoutil.yml down -v                      # Stop and clean up
#
# Health Checks:
#   - Public health endpoints: https://localhost:XXXX/browser/api/v1/health (external clients)
#   - Admin livez endpoints: https://127.0.0.1:9090/admin/api/v1/livez (internal container only, NEVER exposed)
#
# Profiles:
#   - dev: Development mode (default) - SQLite backend, minimal services
#   - demo: Demo mode with seeded data, SQLite backend
#   - ci: CI mode for testing, SQLite backend
#   - postgres: PostgreSQL backend with multiple instances (2x app per service + shared infrastructure)
#
include:
  - path: ../telemetry/compose.yml
  - path: ../postgres/compose.yml

services:
  # Healthcheck for Docker secrets availability.
  # Ephemeral job that validates all secrets are mounted before any service starts.
  healthcheck-secrets:
    image: alpine:latest
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    command: ["sh", "-c", "ls -l /run/secrets/*.secret"]
    networks:
      - cryptoutil-network

  # Build cryptoutil image from source (shared by all services).
  # Ephemeral job that builds Docker image once, reused by all service instances.
  builder-cryptoutil:
    image: cryptoutil:dev
    build:
      context: ../..
      dockerfile: deployments/cryptoutil/Dockerfile
      args:
        APP_VERSION: "dev"
        VCS_REF: "local"
        BUILD_DATE: "2026-02-14T00:00:00Z"
    entrypoint: ["sh", "-c"]
    command: ["echo 'Build completed successfully'"]
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully

  # ========================================================================
  # PKI-CA (Certificate Authority)
  # ========================================================================

  pki-ca-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["pki-ca", "start", "--config=/app/config/pki-ca-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8050:8080"  # HTTPS public
    volumes:
      - ../pki-ca/config/pki-ca-sqlite.yml:/app/config/pki-ca-sqlite.yml:ro
      - ../pki-ca/config/pki-ca-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  pki-ca-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["pki-ca", "start", "--config=/app/config/pki-ca-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8051:8080"
    volumes:
      - ../pki-ca/config/pki-ca-postgres.yml:/app/config/pki-ca-postgres.yml:ro
      - ../pki-ca/config/pki-ca-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  pki-ca-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["pki-ca", "start", "--config=/app/config/pki-ca-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8052:8080"
    volumes:
      - ../pki-ca/config/pki-ca-postgres.yml:/app/config/pki-ca-postgres.yml:ro
      - ../pki-ca/config/pki-ca-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # ========================================================================
  # JOSE-JA (JWK Authority)
  # ========================================================================

  jose-ja-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["jose-ja", "start", "--config=/app/config/jose-ja-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8060:8080"
    volumes:
      - ../jose-ja/config/jose-ja-sqlite.yml:/app/config/jose-ja-sqlite.yml:ro
      - ../jose-ja/config/jose-ja-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  jose-ja-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["jose-ja", "start", "--config=/app/config/jose-ja-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8061:8080"
    volumes:
      - ../jose-ja/config/jose-ja-postgres.yml:/app/config/jose-ja-postgres.yml:ro
      - ../jose-ja/config/jose-ja-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  jose-ja-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["jose-ja", "start", "--config=/app/config/jose-ja-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8062:8080"
    volumes:
      - ../jose-ja/config/jose-ja-postgres.yml:/app/config/jose-ja-postgres.yml:ro
      - ../jose-ja/config/jose-ja-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # ========================================================================
  # CIPHER-IM (Instant Messenger)
  # ========================================================================

  cipher-im-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["cipher-im", "start", "--config=/app/config/cipher-im-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8070:8080"
    volumes:
      - ../cipher-im/config/cipher-im-sqlite.yml:/app/config/cipher-im-sqlite.yml:ro
      - ../cipher-im/config/cipher-im-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  cipher-im-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["cipher-im", "start", "--config=/app/config/cipher-im-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8071:8080"
    volumes:
      - ../cipher-im/config/cipher-im-postgres.yml:/app/config/cipher-im-postgres.yml:ro
      - ../cipher-im/config/cipher-im-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  cipher-im-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["cipher-im", "start", "--config=/app/config/cipher-im-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8072:8080"
    volumes:
      - ../cipher-im/config/cipher-im-postgres.yml:/app/config/cipher-im-postgres.yml:ro
      - ../cipher-im/config/cipher-im-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # ========================================================================
  # SM-KMS (Key Management Service)
  # ========================================================================

  sm-kms-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["sm-kms", "start", "--config=/app/config/sm-kms-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8080:8080"
    volumes:
      - ../sm-kms/config/sm-kms-sqlite.yml:/app/config/sm-kms-sqlite.yml:ro
      - ../sm-kms/config/sm-kms-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  sm-kms-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["sm-kms", "start", "--config=/app/config/sm-kms-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8081:8080"
    volumes:
      - ../sm-kms/config/sm-kms-postgres.yml:/app/config/sm-kms-postgres.yml:ro
      - ../sm-kms/config/sm-kms-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  sm-kms-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["sm-kms", "start", "--config=/app/config/sm-kms-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8082:8080"
    volumes:
      - ../sm-kms/config/sm-kms-postgres.yml:/app/config/sm-kms-postgres.yml:ro
      - ../sm-kms/config/sm-kms-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # ========================================================================
  # IDENTITY-AUTHZ (Authorization Server)
  # ========================================================================

  identity-authz-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["identity-authz", "start", "--config=/app/config/identity-authz-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8100:8080"
    volumes:
      - ../identity-authz/config/identity-authz-sqlite.yml:/app/config/identity-authz-sqlite.yml:ro
      - ../identity-authz/config/identity-authz-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-authz-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-authz", "start", "--config=/app/config/identity-authz-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8101:8080"
    volumes:
      - ../identity-authz/config/identity-authz-postgres.yml:/app/config/identity-authz-postgres.yml:ro
      - ../identity-authz/config/identity-authz-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-authz-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-authz", "start", "--config=/app/config/identity-authz-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8102:8080"
    volumes:
      - ../identity-authz/config/identity-authz-postgres.yml:/app/config/identity-authz-postgres.yml:ro
      - ../identity-authz/config/identity-authz-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # ========================================================================
  # IDENTITY-IDP (Identity Provider)
  # ========================================================================

  identity-idp-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["identity-idp", "start", "--config=/app/config/identity-idp-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8110:8080"
    volumes:
      - ../identity-idp/config/identity-idp-sqlite.yml:/app/config/identity-idp-sqlite.yml:ro
      - ../identity-idp/config/identity-idp-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-idp-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-idp", "start", "--config=/app/config/identity-idp-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8111:8080"
    volumes:
      - ../identity-idp/config/identity-idp-postgres.yml:/app/config/identity-idp-postgres.yml:ro
      - ../identity-idp/config/identity-idp-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-idp-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-idp", "start", "--config=/app/config/identity-idp-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8112:8080"
    volumes:
      - ../identity-idp/config/identity-idp-postgres.yml:/app/config/identity-idp-postgres.yml:ro
      - ../identity-idp/config/identity-idp-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # ========================================================================
  # IDENTITY-RS (Resource Server)
  # ========================================================================

  identity-rs-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["identity-rs", "start", "--config=/app/config/identity-rs-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8120:8080"
    volumes:
      - ../identity-rs/config/identity-rs-sqlite.yml:/app/config/identity-rs-sqlite.yml:ro
      - ../identity-rs/config/identity-rs-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-rs-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-rs", "start", "--config=/app/config/identity-rs-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8121:8080"
    volumes:
      - ../identity-rs/config/identity-rs-postgres.yml:/app/config/identity-rs-postgres.yml:ro
      - ../identity-rs/config/identity-rs-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-rs-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-rs", "start", "--config=/app/config/identity-rs-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8122:8080"
    volumes:
      - ../identity-rs/config/identity-rs-postgres.yml:/app/config/identity-rs-postgres.yml:ro
      - ../identity-rs/config/identity-rs-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # ========================================================================
  # IDENTITY-RP (Relying Party)
  # ========================================================================

  identity-rp-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["identity-rp", "start", "--config=/app/config/identity-rp-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8130:8080"
    volumes:
      - ../identity-rp/config/identity-rp-sqlite.yml:/app/config/identity-rp-sqlite.yml:ro
      - ../identity-rp/config/identity-rp-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-rp-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-rp", "start", "--config=/app/config/identity-rp-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8131:8080"
    volumes:
      - ../identity-rp/config/identity-rp-postgres.yml:/app/config/identity-rp-postgres.yml:ro
      - ../identity-rp/config/identity-rp-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-rp-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-rp", "start", "--config=/app/config/identity-rp-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8132:8080"
    volumes:
      - ../identity-rp/config/identity-rp-postgres.yml:/app/config/identity-rp-postgres.yml:ro
      - ../identity-rp/config/identity-rp-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # ========================================================================
  # IDENTITY-SPA (Single Page App)
  # ========================================================================

  identity-spa-app-sqlite-1:
    profiles: ["dev", "demo", "ci"]
    image: cryptoutil:dev
    command: ["identity-spa", "start", "--config=/app/config/identity-spa-sqlite.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8140:8080"
    volumes:
      - ../identity-spa/config/identity-spa-sqlite.yml:/app/config/identity-spa-sqlite.yml:ro
      - ../identity-spa/config/identity-spa-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-spa-app-postgres-1:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-spa", "start", "--config=/app/config/identity-spa-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8141:8080"
    volumes:
      - ../identity-spa/config/identity-spa-postgres.yml:/app/config/identity-spa-postgres.yml:ro
      - ../identity-spa/config/identity-spa-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  identity-spa-app-postgres-2:
    profiles: ["postgres"]
    image: cryptoutil:dev
    command: ["identity-spa", "start", "--config=/app/config/identity-spa-postgres.yml", "--config=/app/config/common.yml", "--config=/app/otel/otel.yml"]
    working_dir: /tmp
    ports:
      - "8142:8080"
    volumes:
      - ../identity-spa/config/identity-spa-postgres.yml:/app/config/identity-spa-postgres.yml:ro
      - ../identity-spa/config/identity-spa-common.yml:/app/config/common.yml:ro
      - ../telemetry/otel/cryptoutil-otel.yml:/app/otel/otel.yml:ro
    secrets:
      - unseal_1of5.secret
      - unseal_2of5.secret
      - unseal_3of5.secret
      - unseal_4of5.secret
      - unseal_5of5.secret
      - postgres_url.secret
      - postgres_username.secret
      - postgres_password.secret
      - postgres_database.secret
    depends_on:
      healthcheck-secrets:
        condition: service_completed_successfully
      builder-cryptoutil:
        condition: service_completed_successfully
      postgres-leader:
        condition: service_healthy
      opentelemetry-collector-contrib:
        condition: service_started
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O", "/dev/null", "https://127.0.0.1:9090/admin/api/v1/livez"]
      start_period: 60s
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cryptoutil-network
      - postgres-network
      - telemetry-network
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

# ========================================================================
# Networks
# ========================================================================

networks:
  cryptoutil-network:
    driver: bridge
  postgres-network:
    driver: bridge
  telemetry-network:
    driver: bridge

# ========================================================================
# Volumes
# ========================================================================

volumes:
  postgres_leader_data:
  postgres_follower_data:
  citus_coordinator_data:
  citus_worker_1_data:
  citus_worker_2_data:

# ========================================================================
# Secrets
# ========================================================================

secrets:
  unseal_1of5.secret:
    file: ./secrets/unseal_1of5.secret
  unseal_2of5.secret:
    file: ./secrets/unseal_2of5.secret
  unseal_3of5.secret:
    file: ./secrets/unseal_3of5.secret
  unseal_4of5.secret:
    file: ./secrets/unseal_4of5.secret
  unseal_5of5.secret:
    file: ./secrets/unseal_5of5.secret
  postgres_url.secret:
    file: ./secrets/postgres_url.secret
  postgres_username.secret:
    file: ./secrets/postgres_username.secret
  postgres_password.secret:
    file: ./secrets/postgres_password.secret
  postgres_database.secret:
    file: ./secrets/postgres_database.secret
