# CA PostgreSQL Instance 2 Configuration
# Used by: pki-ca-app-postgresql-2 service in compose.yml
#
# This file contains instance-specific settings.
# Common settings are in pki-ca-app-common.yml.

# Service name for telemetry
service-name: "pki-ca-postgresql-2"

# Public API port (mapped externally as 8303)
bind-public-port: 8080

# Database backend: URL passed via -u flag in compose.yml (file:///run/secrets/postgres-url.secret)

# CORS origins (specific to this instance)
cors-origins:
  - "https://localhost:8303"
  - "https://127.0.0.1:8303"

# OTLP telemetry service name (unique per instance)
otlp-service-name: "pki-ca-postgresql-2"
otlp-hostname: "pki-ca-postgresql-2"
