services:

  cryptoutil_sqlite:
    build:
      context: ../..
      dockerfile: deployments/Dockerfile
    command: ["server", "start", "--config=/app/sqlite.yml"]
    environment:
      - CRYPTOUTIL_OTLP=true
      - CRYPTOUTIL_OTLP_CONSOLE=false
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://opentelemetry-collector:4317
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://opentelemetry-collector:4317
      - OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://opentelemetry-collector:4317
      - OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://opentelemetry-collector:4317
      - OTEL_SERVICE_NAME=cryptoutil-sqlite
    ports:
      - "8090:8080"
    volumes:
      - ./cryptoutil/sqlite.yml:/app/sqlite.yml:ro
    depends_on:
      opentelemetry-collector:
        condition: service_started
    networks:
      - cryptoutil-network

  cryptoutil_postgres:
    build:
      context: ../..
      dockerfile: deployments/Dockerfile
    command: ["server", "start", "--config=/app/postgresql.yml"]
    environment:
      - CRYPTOUTIL_OTLP=true
      - CRYPTOUTIL_OTLP_CONSOLE=false
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://opentelemetry-collector:4317
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://opentelemetry-collector:4317
      - OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://opentelemetry-collector:4317
      - OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://opentelemetry-collector:4317
      - OTEL_SERVICE_NAME=cryptoutil-postgres
      - CRYPTOUTIL_DATABASE_URL=postgres://USR:PWD@postgres:5432/DB?sslmode=disable
    ports:
      - "8080-8089:8080"
    volumes:
      - ./cryptoutil/postgresql.yml:/app/postgresql.yml:ro
      - postgres_app:/app/data
    secrets:
      - cryptoutil_database-url.secret
      - cryptoutil_unseal_1of5.secret
      - cryptoutil_unseal_2of5.secret
      - cryptoutil_unseal_3of5.secret
      - cryptoutil_unseal_4of5.secret
      - cryptoutil_unseal_5of5.secret
    depends_on:
      postgres:
        condition: service_healthy
      opentelemetry-collector:
        condition: service_started
    networks:
      - cryptoutil-network

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: USR
      POSTGRES_PASSWORD: PWD
      POSTGRES_DB: DB
    shm_size: 256mb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$(cat /run/secrets/postgres_username.secret) -d $$(cat /run/secrets/postgres_database.secret)"]
      start_period: 5s
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - cryptoutil-network

  opentelemetry-collector:
    image: otel/opentelemetry-collector-contrib:latest
    command: ["--config=/etc/otel-collector-config.yaml"]
    volumes:
      - ./otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
    ports:
      - "4317:4317"   # OTLP gRPC
      - "4318:4318"   # OTLP HTTP
      - "8888:8888"   # Metrics endpoint
      - "8889:8889"   # Prometheus metrics
    networks:
      - cryptoutil-network

  grafana:
    image: grafana/grafana:latest
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - opentelemetry-collector
    networks:
      - cryptoutil-network

networks:
  cryptoutil-network:
    driver: bridge

volumes:
  postgres_data:
    name: cryptoutil_postgres_volume
  grafana_data:
    name: cryptoutil_grafana_volume
  postgres_app:
    name: cryptoutil_postgres_app

secrets:

  postgres_username.secret:
    file: ./postgres/postgres_username.secret
  postgres_password.secret:
    file: ./postgres/postgres_password.secret
  postgres_database.secret:
    file: ./postgres/postgres_database.secret

  cryptoutil_database-url.secret:
    file: ./cryptoutil/cryptoutil_database_url.secret

  cryptoutil_unseal_1of5.secret:
    file: ./cryptoutil/cryptoutil_unseal_1of5.secret
  cryptoutil_unseal_2of5.secret:
    file: ./cryptoutil/cryptoutil_unseal_2of5.secret
  cryptoutil_unseal_3of5.secret:
    file: ./cryptoutil/cryptoutil_unseal_3of5.secret
  cryptoutil_unseal_4of5.secret:
    file: ./cryptoutil/cryptoutil_unseal_4of5.secret
  cryptoutil_unseal_5of5.secret:
    file: ./cryptoutil/cryptoutil_unseal_5of5.secret
