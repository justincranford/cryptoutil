# CRITICAL: ALL settings in this file MUST BE UNIQUE and CORRESPOND TO the 'identity-idp-app-postgres-1' service name in compose.yml

# CORS configuration - HTTPS origins only
cors-origins:
  - "https://localhost:18301"
  - "https://127.0.0.1:18301"
  - "https://[::1]:18301"

otlp-service: identity-idp-postgres-1
otlp-hostname: identity-idp-postgres-1
