---
# Database password secret
apiVersion: v1
kind: Secret
metadata:
  name: ca-db-credentials
  namespace: ca-system
  labels:
    app.kubernetes.io/name: ca-database
    app.kubernetes.io/component: database
type: Opaque
stringData:
  # IMPORTANT: Replace with actual secure values before deployment
  password: "CHANGE_ME_IN_PRODUCTION"
---
# CA key password secret
apiVersion: v1
kind: Secret
metadata:
  name: ca-key-credentials
  namespace: ca-system
  labels:
    app.kubernetes.io/name: ca-service
    app.kubernetes.io/component: ca
type: Opaque
stringData:
  # IMPORTANT: Replace with actual secure values before deployment
  password: "CHANGE_ME_IN_PRODUCTION"
---
# OCSP key password secret
apiVersion: v1
kind: Secret
metadata:
  name: ocsp-key-credentials
  namespace: ca-system
  labels:
    app.kubernetes.io/name: ocsp-responder
    app.kubernetes.io/component: ocsp
type: Opaque
stringData:
  # IMPORTANT: Replace with actual secure values before deployment
  password: "CHANGE_ME_IN_PRODUCTION"
