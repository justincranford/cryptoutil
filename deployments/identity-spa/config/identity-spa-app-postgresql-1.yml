# CRITICAL: ALL settings in this file MUST BE UNIQUE and CORRESPOND TO the 'identity-spa-app-postgres-1' service name in compose.yml

# CORS configuration - HTTPS origins only
cors-origins:
  - "https://localhost:18601"
  - "https://127.0.0.1:18601"
  - "https://[::1]:18601"

otlp-service: identity-spa-postgres-1
otlp-hostname: identity-spa-postgres-1
