# CRITICAL: ALL settings in this file MUST BE UNIQUE and CORRESPOND TO the 'identity-rs-app-postgres-1' service name in compose.yml

# CORS configuration - HTTPS origins only
cors-origins:
  - "https://localhost:18401"
  - "https://127.0.0.1:18401"
  - "https://[::1]:18401"

otlp-service: identity-rs-postgres-1
otlp-hostname: identity-rs-postgres-1
