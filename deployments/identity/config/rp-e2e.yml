# Relying Party Server E2E Test Configuration

server:
  bind-public-address: 0.0.0.0
  bind-public-port: 18300
  bind-admin-address: 0.0.0.0
  bind-admin-port: 9090

database:
  type: sqlite
  dsn: ":memory:"
  auto-migrate: true

telemetry:
  enabled: true
  otlp-endpoint: "opentelemetry-collector-contrib:4317"
  service-name: identity-rp-e2e

# Identity-rp specific settings
authz-url: "http://identity-authz-e2e:18000"
idp-url: "http://identity-idp-e2e:18100"

logging:
  level: debug
