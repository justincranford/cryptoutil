# Identity Authorization Server Configuration
# This file provides the default configuration for the Identity AuthZ server.
# Override with environment-specific configs in deployments/identity-authz/config/

# Server binding
bind-public-address: "127.0.0.1"
bind-public-port: 8400
bind-admin-address: "127.0.0.1"
bind-admin-port: 9090

# TLS configuration for HTTPS
tls-enabled: true
tls-cert-file: "" # Set in deployment config
tls-key-file: "" # Set in deployment config

# CORS configuration - HTTPS origins only
cors-origins:
  - "https://localhost:8400"
  - "https://127.0.0.1:8400"

# OTLP telemetry
otlp-enabled: false
otlp-endpoint: ""
otlp-service: "identity-authz"
otlp-hostname: "localhost"

# Logging
log-level: "INFO"

# Database (in-memory SQLite by default)
database-url: "sqlite://file::memory:?cache=shared"

# OAuth 2.1 Authorization Server settings
issuer: "https://localhost:8400"
token-lifetime: 3600
refresh-token-lifetime: 86400
authorization-code-ttl: 600
enable-discovery: true
enable-dynamic-registration: false
