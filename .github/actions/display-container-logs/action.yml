name: 'Display Container Logs'
description: 'Display and extract container logs from workflow reports'
inputs:
  log-directory:
    description: 'Directory containing the log archives'
    required: true
    default: './workflow-reports/e2e'
runs:
  using: 'composite'
  steps:
    - name: Display container logs
      shell: bash
      run: |
        echo "📋 Displaying captured container logs..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        LOG_DIR="${{ inputs.log-directory }}"

        # Check if directory exists
        if [ ! -d "$LOG_DIR" ]; then
          echo "❌ Log directory $LOG_DIR does not exist"
          echo "📋 Checking if workflow-reports directory exists..."
          if [ -d "./workflow-reports" ]; then
            echo "✅ workflow-reports directory exists"
            ls -la ./workflow-reports/
          else
            echo "❌ workflow-reports directory does not exist"
          fi
          exit 1
        fi

        echo "✅ Found log directory: $LOG_DIR"

        # List all files in the directory with full details
        echo "📁 Directory contents (detailed):"
        ls -la "$LOG_DIR/" || echo "❌ Failed to list directory contents"

        # Check for zip files specifically
        echo "🔍 Looking for zip files..."
        ZIP_COUNT=$(find "$LOG_DIR" -name "*.zip" 2>/dev/null | wc -l)
        echo "Found $ZIP_COUNT zip files"

        if [ "$ZIP_COUNT" -eq 0 ]; then
          echo "❌ No container log archives found in $LOG_DIR"
          echo "📋 Checking for any log files directly..."
          LOG_FILES=$(find "$LOG_DIR" -name "*.log" 2>/dev/null || true)
          if [ -n "$LOG_FILES" ]; then
            echo "📄 Found log files:"
            echo "$LOG_FILES"
            echo "📖 Displaying log file contents (first 20 lines each):"
            for log_file in $LOG_FILES; do
              echo "🔍 Contents of: $(basename "$log_file")"
              echo "----------------------------------------"
              head -20 "$log_file" || echo "❌ Failed to read $log_file"
              echo "----------------------------------------"
              echo ""
            done
          else
            echo "❌ No log files found either"
            echo "🔍 Checking for any files at all..."
            ANY_FILES=$(find "$LOG_DIR" -type f 2>/dev/null | wc -l)
            echo "Found $ANY_FILES files total in $LOG_DIR"
            if [ "$ANY_FILES" -gt 0 ]; then
              echo "📄 All files in directory:"
              find "$LOG_DIR" -type f -exec ls -la {} \;
            fi
          fi
          exit 1
        fi

        echo "✅ Container log archives found"

        # Get the most recent zip file
        LATEST_ZIP=$(find "$LOG_DIR" -name "*.zip" -printf '%T@ %p\n' 2>/dev/null | sort -n | tail -1 | cut -d' ' -f2-)
        if [ -z "$LATEST_ZIP" ]; then
          LATEST_ZIP=$(ls -t ${LOG_DIR}/*.zip 2>/dev/null | head -1)
        fi

        if [ -z "$LATEST_ZIP" ] || [ ! -f "$LATEST_ZIP" ]; then
          echo "❌ Could not determine latest zip file"
          exit 1
        fi

        echo "📦 Processing archive: $LATEST_ZIP"
        echo "📊 Archive size: $(stat -c%s "$LATEST_ZIP" 2>/dev/null || echo "unknown") bytes"

        # Check if unzip is available
        if ! command -v unzip &> /dev/null; then
          echo "❌ unzip command not available"

          # Try using python to extract zip
          if command -v python3 &> /dev/null; then
            echo "🐍 Using Python to extract zip file..."
            python3 -c "
            import zipfile
            import os
            import sys

            zip_path = '$LATEST_ZIP'
            extract_dir = '/tmp/container_logs_' + str(os.getpid())

            print(f'Extracting {zip_path} to {extract_dir}')

            try:
              os.makedirs(extract_dir, exist_ok=True)
              with zipfile.ZipFile(zip_path, 'r') as zip_ref:
                file_list = zip_ref.namelist()
                print(f'Archive contains {len(file_list)} files:')
                for file in file_list[:10]:  # Show first 10 files
                  print(f'  {file}')
                if len(file_list) > 10:
                  print(f'  ... and {len(file_list) - 10} more files')

                zip_ref.extractall(extract_dir)
                print(f'✅ Successfully extracted to {extract_dir}')

                # Display log files
                log_count = 0
                for root, dirs, files in os.walk(extract_dir):
                  for file in files:
                    if file.endswith('.log'):
                      log_count += 1
                      file_path = os.path.join(root, file)
                      print(f'🔍 Logs for service: {file[:-4]}')  # Remove .log extension
                      print('----------------------------------------')
                      with open(file_path, 'r') as f:
                        content = f.read()
                        # Show first 2000 chars to avoid too much output
                        if len(content) > 2000:
                          print(content[:2000] + '...')
                          print(f'(truncated, {len(content)} total characters)')
                        else:
                          print(content)
                      print('----------------------------------------')
                      print('')

                if log_count == 0:
                  print('⚠️ No .log files found in archive')
                  print('All files in archive:')
                  for root, dirs, files in os.walk(extract_dir):
                    for file in files:
                      file_path = os.path.join(root, file)
                      print(f'  {os.path.relpath(file_path, extract_dir)}')

            except Exception as e:
              print(f'❌ Failed to extract with Python: {e}')
              import traceback
              traceback.print_exc()
              sys.exit(1)
            "
          else
            echo "❌ Neither unzip nor python3 available for extraction"
            exit 1
          fi
        else
          echo "✅ unzip command available"

          # Create temp directory for extraction
          TEMP_DIR=$(mktemp -d)
          echo "📁 Extracting to: $TEMP_DIR"

          # Extract the zip file with verbose output
          echo "🔄 Extracting zip file..."
          if unzip -l "$LATEST_ZIP"; then
            echo "✅ Zip file listing successful"

            if unzip -q "$LATEST_ZIP" -d "$TEMP_DIR"; then
              echo "✅ Successfully extracted zip file"

              # List extracted files
              echo "📄 Extracted files:"
              find "$TEMP_DIR" -type f | head -20

              # Display logs for each service
              LOG_COUNT=0
              while IFS= read -r -d '' log_file; do
                LOG_COUNT=$((LOG_COUNT + 1))
                SERVICE_NAME=$(basename "$log_file" .log)
                echo "🔍 Logs for service: $SERVICE_NAME"
                echo "----------------------------------------"

                # Show first 50 lines to avoid too much output
                head -50 "$log_file"

                # If file is longer, indicate truncation
                LINE_COUNT=$(wc -l < "$log_file" 2>/dev/null || echo 0)
                if [ "$LINE_COUNT" -gt 50 ]; then
                  echo "... (truncated, $LINE_COUNT total lines)"
                fi

                echo "----------------------------------------"
                echo ""
              done < <(find "$TEMP_DIR" -name "*.log" -print0)

              if [ "$LOG_COUNT" -eq 0 ]; then
                echo "⚠️ No .log files found in extracted archive"
                echo "📄 All extracted files:"
                find "$TEMP_DIR" -type f -exec ls -la {} \;
              else
                echo "✅ Displayed logs for $LOG_COUNT services"
              fi

            else
              echo "❌ Failed to extract zip file"
              unzip -v "$LATEST_ZIP" || echo "❌ Could not get zip file info"
              exit 1
            fi
          else
            echo "❌ Failed to list zip file contents"
            exit 1
          fi

          # Clean up temp directory
          rm -rf "$TEMP_DIR"
        fi
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ Container log display completed in: ${DURATION}s"
