name: 'Docker Image Pre-pull (Parallel)'
description: 'Pre-pull Docker images in parallel to speed up workflow execution'
inputs:
  images:
    description: 'JSON array of Docker images to pull (optional - uses common set if not specified)'
    required: false
    default: ''
  use-common-images:
    description: 'Whether to include the common cryptoutil image set'
    required: false
    default: 'true'
  additional-images:
    description: 'JSON array of additional Docker images to pull'
    required: false
    default: '[]'
  verify-docker-environment:
    description: 'Whether to verify Docker and Docker Compose availability and configuration'
    required: false
    default: 'false'
  compose-file:
    description: 'Path to Docker Compose file for validation (required if verify-docker-environment is true)'
    required: false
    default: './deployments/compose/compose.yml'
runs:
  using: 'composite'
  steps:
    - name: Verify Docker environment
      if: inputs.verify-docker-environment == 'true'
      shell: bash
      run: |
        echo "🔍 Verifying Docker environment..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

        # Check if Docker is available
        if ! command -v docker &> /dev/null; then
          echo "❌ Docker command not found"
          exit 1
        fi
        echo "✅ Docker available: $(docker --version)"

        # Check if Docker Compose is available
        if ! command -v docker &> /dev/null || ! docker compose version &> /dev/null; then
          echo "❌ Docker Compose not available"
          exit 1
        fi
        echo "✅ Docker Compose available: $(docker compose version)"

        # Check if compose file exists
        COMPOSE_FILE="${{ inputs.compose-file }}"
        if [ ! -f "$COMPOSE_FILE" ]; then
          echo "❌ Compose file not found: $COMPOSE_FILE"
          exit 1
        fi
        echo "✅ Compose file exists: $COMPOSE_FILE"

        # List services in compose file
        echo "📋 Services in compose file:"
        docker compose -f "$COMPOSE_FILE" config --services
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ Docker environment verification completed in: ${DURATION}s"

    - name: Pre-pull Docker images (parallel)
      shell: bash
      run: |
        echo "🐳 Starting Docker image pre-pull..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

        # Define common cryptoutil images
        COMMON_IMAGES='[
          "postgres:18",
          "otel/opentelemetry-collector-contrib:latest",
          "grafana/otel-lgtm:latest",
          "alpine:latest",
          "golang:1.25.3",
          "alpine:3.19"
        ]'

        # Determine which images to pull
        if [ -n "${{ inputs.images }}" ]; then
          # Custom image list provided
          IMAGES=$(echo '${{ inputs.images }}' | jq -r '.[]')
          echo "📋 Using custom image list (${{ inputs.images }})"
        elif [ "${{ inputs.use-common-images }}" = "false" ]; then
          # Only additional images
          IMAGES=$(echo '${{ inputs.additional-images }}' | jq -r '.[]')
          echo "📋 Using only additional images"
        else
          # Use common images + additional images
          if [ "${{ inputs.additional-images }}" != "[]" ] && [ -n "${{ inputs.additional-images }}" ]; then
            IMAGES=$(echo "$COMMON_IMAGES" | jq -r '.[]' && echo '${{ inputs.additional-images }}' | jq -r '.[]')
            echo "📋 Using common images + additional images"
          else
            IMAGES=$(echo "$COMMON_IMAGES" | jq -r '.[]')
            echo "📋 Using common cryptoutil images"
          fi
        fi

        # Remove duplicates and pull all images concurrently
        UNIQUE_IMAGES=$(echo "$IMAGES" | sort | uniq)
        echo "📦 Will pull $(echo "$UNIQUE_IMAGES" | wc -l) unique images"

        for image in $UNIQUE_IMAGES; do
          echo "Pulling $image..."
          docker pull "$image" &
        done

        # Wait for all pulls to complete
        wait

        echo "✅ All images pre-pulled successfully"
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ Docker image pre-pull completed in: ${DURATION}s"
