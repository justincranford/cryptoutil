# $schema: https://json.schemastore.org/github-workflow.json
# https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax

# WORKFLOW: CI - Quality Testing
# PURPOSE: Enforce code quality, linting, formatting, and build validation
# DEPENDENCIES: None (runs independently without external services)
# EXPECTED DURATION: 3-5 minutes (linting + builds)
# TIMEOUT: 15 minutes (generous for GitHub Actions shared CPU latency)
# CRITICAL PATH: golangci-lint (slowest step, ~2-3 minutes)

name: CI - Quality Testing
# CROSS-PLATFORM COMPATIBILITY: All file references in this workflow MUST use relative paths
# to support both GitHub Actions Ubuntu runners and Windows `act` local runner testing.
# Absolute paths (e.g., C:\...) break cross-platform compatibility.
# Example: Use './deployments/cryptoutil-suite/compose.yml'

on:
  push:
    branches: [ main, develop ]
    # SHARED PATHS-IGNORE CONFIGURATION - Keep synchronized with pull_request below
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
      - '.github/copilot-instructions.md'
      - '.github/instructions/**'
      - 'workflow-reports/**'
      - 'nohup.out'
      - 'LICENSE'
      - '.editorconfig'
      - '.gitignore'
      - '.gitattributes'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/pull_request_template.md'
      - '.github/dependabot.yml'
      - '**/*.log'
      - '**/*.sarif'
  pull_request:
    branches: [ main, develop ]
    # SHARED PATHS-IGNORE CONFIGURATION - Keep synchronized with push above
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
      - '.github/copilot-instructions.md'
      - '.github/instructions/**'
      - 'workflow-reports/**'
      - 'nohup.out'
      - 'LICENSE'
      - '.editorconfig'
      - '.gitignore'
      - '.gitattributes'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/pull_request_template.md'
      - '.github/dependabot.yml'
      - '**/*.log'
      - '**/*.sarif'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Code Quality and Formatting
  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      # https://github.com/actions/checkout/releases (v6.0.0, 2025-11-11)
      uses: actions/checkout@v6.0.0
      with:
        sparse-checkout-cone-mode: false  # Disabled for cross-platform compatibility between GitHub Actions and act local runner

    - name: Begin Workflow Job
      id: begin-workflow-job
      uses: ./.github/actions/workflow-job-begin

    - name: Set up Go and validate dependencies
      uses: ./.github/actions/go-setup
      with:
        go-version: ${{ steps.begin-workflow-job.outputs.go-version }}
        go-mod-download: true
        go-mod-verify: true
        go-mod-tidy: true

    - name: Run golangci-lint
      uses: ./.github/actions/golangci-lint

    - name: Run custom cicd linting and validation
      uses: ./.github/actions/custom-cicd-lint

    - name: Code quality summary
      if: always()
      uses: ./.github/actions/workflow-job-end

  # Build and Container Security
  build:
    name: Build & Container Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      security-events: write
    steps:
      - name: Checkout code
        # https://github.com/actions/checkout/releases (v6.0.0, 2025-11-11)
        uses: actions/checkout@v6.0.0
        with:
          sparse-checkout-cone-mode: false  # Disabled for cross-platform compatibility between GitHub Actions and act local runner

      - name: Begin Workflow Job
        id: begin-workflow-job
        uses: ./.github/actions/workflow-job-begin

      - name: Set up Go and validate dependencies
        uses: ./.github/actions/go-setup
        with:
          go-version: ${{ steps.begin-workflow-job.outputs.go-version }}
          go-mod-download: true
          go-mod-verify: true
          go-mod-tidy: true

      - name: Build binary
        run: |
          echo "🔨 Building application binary..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          # Build with static linking and minimal symbol stripping for CI/testing
          # -s: Strip symbol table for smaller binary size (keeps debug symbols for diagnostics)
          # -extldflags '-static': Force static linking for maximum portability
          # Performance and diagnostics prioritized over size; debug symbols retained for troubleshooting
          go build -v -ldflags="-s -extldflags '-static'" -o cryptoutil ./cmd/cryptoutil
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ Binary build completed in: ${DURATION}s"

      - name: Test binary
        run: |
          echo "🧪 Testing binary execution..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          # Test that the binary was built successfully
          ls -la ./cryptoutil
          file ./cryptoutil
          # Test basic execution (expect it to show usage and exit with error since no valid command given)
          ./cryptoutil || echo "Binary executed successfully (expected error due to no command)"
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ Binary test completed in: ${DURATION}s"

      - name: Set up Docker Buildx
        run: |
          echo "🐳 Setting up Docker Buildx..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          docker buildx create --use
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ Docker Buildx setup completed in: ${DURATION}s"

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        # https://github.com/docker/login-action/releases (v3.6.0, 2025-11-11)
        uses: docker/login-action@v3.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        # https://github.com/docker/metadata-action/releases (v5.9.0, 2025-11-11)
        uses: docker/metadata-action@v5.9.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix=sha-

      - name: Build Docker image
        run: |
          echo "🏗️ Building Docker image..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

          # Build the tag arguments using process substitution to avoid subshell variable scoping issues
          TAG_ARGS=""
          while IFS= read -r tag; do
            if [ -n "$tag" ]; then
              TAG_ARGS="$TAG_ARGS --tag $tag"
            fi
          done <<< "${{ steps.meta.outputs.tags }}"

          docker buildx build \
            --load \
            $TAG_ARGS \
            --label "org.opencontainers.image.created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" \
            --label "org.opencontainers.image.revision=${{ github.sha }}" \
            --label "org.opencontainers.image.version=${{ github.sha }}" \
            --build-arg APP_VERSION=${{ github.sha }} \
            --build-arg VCS_REF=${{ github.sha }} \
            --build-arg BUILD_DATE=1970-01-01T00:00:00Z \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            -f ./deployments/cryptoutil-suite/Dockerfile \
            .
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ Docker image build completed in: ${DURATION}s"

      - name: Get first Docker tag for Trivy scan
        id: first-tag
        run: echo "tag=$(echo '${{ steps.meta.outputs.tags }}' | head -n1)" >> $GITHUB_OUTPUT

      - name: Scan Docker image for vulnerabilities
        uses: ./.github/actions/security-scan-trivy2
        with:
          scan-name: 'docker-image'
          scan-type: 'image'
          scan-ref: ${{ steps.first-tag.outputs.tag }}
          trivy-sarif-output: 'trivy-image.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW,UNKNOWN'

      # Docker Scout vulnerability scanning
      - name: Check Docker Scout authentication
        id: scout-auth
        run: |
          echo "🔍 Checking Docker Scout authentication..."
          if docker scout version >/dev/null 2>&1; then
            echo "authenticated=true" >> $GITHUB_OUTPUT
            echo "✅ Docker Scout is authenticated and available"
          else
            echo "authenticated=false" >> $GITHUB_OUTPUT
            echo "❌ Docker Scout authentication failed - skipping Docker Scout scans"
          fi
        continue-on-error: true

      - name: Enable Docker Scout
        # https://github.com/docker/scout-action/releases (v1.18.2, 2025-11-20)
        uses: docker/scout-action@v1.18.2
        with:
          command: version
        if: steps.scout-auth.outputs.authenticated == 'true'

      - name: Docker Scout - Quick Overview
        # https://github.com/docker/scout-action/releases (v1.18.2, 2025-11-20)
        uses: docker/scout-action@v1.18.2
        with:
          command: quickview
          image: ${{ steps.first-tag.outputs.tag }}
          only-severities: critical,high,medium
          write-comment: false
        if: steps.scout-auth.outputs.authenticated == 'true'

      - name: Docker Scout - CVE Analysis
        # https://github.com/docker/scout-action/releases (v1.18.2, 2025-11-20)
        uses: docker/scout-action@v1.18.2
        with:
          command: cves
          image: ${{ steps.first-tag.outputs.tag }}
          format: sarif
          output: docker-scout-cves.sarif
          only-severities: critical,high,medium
        continue-on-error: true
        if: steps.scout-auth.outputs.authenticated == 'true'

      - name: Upload Docker Scout CVE results
        # https://github.com/github/codeql-action/releases (v3.28.19, 2025-11-06)
        uses: github/codeql-action/upload-sarif@v3.28.19
        with:
          sarif_file: 'docker-scout-cves.sarif'
        if: steps.scout-auth.outputs.authenticated == 'true' && hashFiles('docker-scout-cves.sarif') != ''

      - name: Docker Scout - Security Recommendations
        # https://github.com/docker/scout-action/releases (v1.18.2, 2025-11-20)
        uses: docker/scout-action@v1.18.2
        with:
          command: recommendations
          image: ${{ steps.first-tag.outputs.tag }}
          only-severities: critical,high,medium
        continue-on-error: true
        if: steps.scout-auth.outputs.authenticated == 'true'

      - name: Push Docker image
        if: github.event_name != 'pull_request'
        run: |
          echo "📤 Pushing Docker image..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

          # Build the tag arguments using process substitution to avoid subshell variable scoping issues
          TAG_ARGS=""
          while IFS= read -r tag; do
            if [ -n "$tag" ]; then
              TAG_ARGS="$TAG_ARGS --tag $tag"
            fi
          done <<< "${{ steps.meta.outputs.tags }}"

          docker buildx build \
            --push \
            $TAG_ARGS \
            --label "org.opencontainers.image.created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" \
            --label "org.opencontainers.image.revision=${{ github.sha }}" \
            --label "org.opencontainers.image.version=${{ github.sha }}" \
            --build-arg APP_VERSION=${{ github.sha }} \
            --build-arg VCS_REF=${{ github.sha }} \
            --build-arg BUILD_DATE=1970-01-01T00:00:00Z \
            -f ./deployments/cryptoutil-suite/Dockerfile \
            .
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ Docker image push completed in: ${DURATION}s"

      - name: Build summary
        if: always()
        uses: ./.github/actions/workflow-job-end

  # SBOM Generation
  sbom:
    name: Generate SBOM
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        # https://github.com/actions/checkout/releases (v6.0.0, 2025-11-11)
        uses: actions/checkout@v6.0.0
        with:
          sparse-checkout-cone-mode: false  # Disabled for cross-platform compatibility between GitHub Actions and act local runner

      - name: Begin Workflow Job
        id: begin-workflow-job
        uses: ./.github/actions/workflow-job-begin

      - name: Set up Go and validate dependencies
        uses: ./.github/actions/go-setup
        with:
          go-version: ${{ steps.begin-workflow-job.outputs.go-version }}
          go-mod-download: true
          go-mod-verify: true
          go-mod-tidy: true

      - name: Install syft
        run: |
          echo "📦 Installing syft for SBOM generation..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

          # Download and install syft
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin

          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ Syft installation completed in: ${DURATION}s"

      - name: Generate SBOM with syft
        run: |
          echo "📦 Generating SBOM with syft..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          syft packages . --output spdx-json=sbom.spdx.json
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ SBOM generation completed in: ${DURATION}s"

      - name: Upload SBOM
        # https://github.com/actions/upload-artifact/releases (v5.0.0, 2025-04-21)
        uses: actions/upload-artifact@v5.0.0
        with:
          name: sbom
          path: sbom.spdx.json
          retention-days: 1

      - name: Upload SBOM to dependency graph
        # https://github.com/anchore/sbom-action/releases (v0.20.10, 2025-10-23)
        uses: anchore/sbom-action@v0.20.10
        with:
          path: ./
          format: spdx-json
          output-file: sbom.spdx.json
          upload-artifact: false
          upload-release-assets: false
          dependency-snapshot: true

      - name: SBOM summary
        if: always()
        uses: ./.github/actions/workflow-job-end

  # Mutation Testing - Quality Assessment
  mutation-testing:
    name: Mutation Testing
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'  # Only on main branch
    steps:
      - name: Checkout code
        # https://github.com/actions/checkout/releases (v6.0.0, 2025-11-11)
        uses: actions/checkout@v6.0.0
        with:
          sparse-checkout-cone-mode: false  # Disabled for cross-platform compatibility between GitHub Actions and act local runner

      - name: Begin Workflow Job
        id: begin-workflow-job
        uses: ./.github/actions/workflow-job-begin

      - name: Set up Go and validate dependencies
        uses: ./.github/actions/go-setup
        with:
          go-version: ${{ steps.begin-workflow-job.outputs.go-version }}
          go-mod-download: true
          go-mod-verify: true
          go-mod-tidy: true

      - name: Install Gremlins
        run: |
          echo "🧬 Installing Gremlins mutation testing tool..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go install github.com/go-gremlins/gremlins/cmd/gremlins@latest
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ Gremlins installation completed in: ${DURATION}s"

      - name: Run mutation testing on high-coverage packages
        run: |
          echo "🧪 Running mutation testing on packages with high test coverage..."
          OVERALL_START=$(date +%s)
          echo "📅 Mutation testing start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

          # Test packages with excellent coverage
          HIGH_COVERAGE_PACKAGES=(
            "./internal/common/util/datetime/"
            "./internal/common/util/thread/"
            "./internal/common/util/sysinfo/"
          )

          TOTAL_KILLED=0
          TOTAL_LIVED=0
          TOTAL_NOT_COVERED=0
          MUTATION_REPORTS=""

          for package in "${HIGH_COVERAGE_PACKAGES[@]}"; do
            echo "🎯 Testing package: $package"
            START_TIME=$(date +%s)

            # Run mutation testing with reasonable settings
            OUTPUT=$(gremlins unleash "$package" \
              --workers 2 \
              --timeout-coefficient 3 \
              --threshold-efficacy 70.0 \
              --threshold-mcover 60.0 \
              --output "mutation-$(basename "$package").json" 2>&1) || true

            END_TIME=$(date +%s)
            DURATION=$((END_TIME - START_TIME))
            echo "⏱️ Package $package completed in: ${DURATION}s"

            echo "$OUTPUT"
            MUTATION_REPORTS="$MUTATION_REPORTS mutation-$(basename "$package").json"

            # Extract metrics from output
            KILLED=$(echo "$OUTPUT" | grep -o "Killed: [0-9]*" | grep -o "[0-9]*" || echo "0")
            LIVED=$(echo "$OUTPUT" | grep -o "Lived: [0-9]*" | grep -o "[0-9]*" || echo "0")
            NOT_COVERED=$(echo "$OUTPUT" | grep -o "Not covered: [0-9]*" | grep -o "[0-9]*" || echo "0")

            TOTAL_KILLED=$((TOTAL_KILLED + KILLED))
            TOTAL_LIVED=$((TOTAL_LIVED + LIVED))
            TOTAL_NOT_COVERED=$((TOTAL_NOT_COVERED + NOT_COVERED))

            echo "Package results - Killed: $KILLED, Lived: $LIVED, Not covered: $NOT_COVERED"
            echo ""
          done

          OVERALL_END=$(date +%s)
          TOTAL_DURATION=$((OVERALL_END - OVERALL_START))

          # Calculate overall metrics
          TOTAL_TESTED=$((TOTAL_KILLED + TOTAL_LIVED))
          if [ $TOTAL_TESTED -gt 0 ]; then
            EFFICACY=$(( (TOTAL_KILLED * 100) / TOTAL_TESTED ))
          else
            EFFICACY=0
          fi

          echo "📊 MUTATION TESTING SUMMARY"
          echo "========================="
          echo "Total Killed: $TOTAL_KILLED"
          echo "Total Lived: $TOTAL_LIVED"
          echo "Total Not Covered: $TOTAL_NOT_COVERED"
          echo "Test Efficacy: ${EFFICACY}%"
          echo "Total Duration: ${TOTAL_DURATION}s"

          # Set up summary for GitHub
          echo "## 🧬 Mutation Testing Results" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🎯 Killed | $TOTAL_KILLED |" >> $GITHUB_STEP_SUMMARY
          echo "| 🧟 Lived | $TOTAL_LIVED |" >> $GITHUB_STEP_SUMMARY
          echo "| 🚫 Not Covered | $TOTAL_NOT_COVERED |" >> $GITHUB_STEP_SUMMARY
          echo "| 📈 Test Efficacy | ${EFFICACY}% |" >> $GITHUB_STEP_SUMMARY
          echo "| ⏱️ Total Duration | ${TOTAL_DURATION}s |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ $TOTAL_LIVED -gt 0 ]; then
            echo "⚠️ **Warning**: $TOTAL_LIVED mutations survived testing" >> $GITHUB_STEP_SUMMARY
            echo "This indicates potential gaps in test coverage or assertion quality." >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **Excellent**: All testable mutations were killed by the test suite!" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload mutation testing reports
        # https://github.com/actions/upload-artifact/releases (v5.0.0, 2025-04-21)
        uses: actions/upload-artifact@v5.0.0
        if: always()
        with:
          name: mutation-testing-reports
          path: mutation-*.json
          retention-days: 1

      - name: Mutation testing summary
        if: always()
        uses: ./.github/actions/workflow-job-end

  # Identity Requirements Coverage Check
  # TEMPORARILY DISABLED: The requirements.yml file and docs/02-identityV2 directory
  # do not exist yet. Re-enable when identity requirements traceability infrastructure is created.
  # identity-requirements:
  #   name: Identity Requirements Coverage
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       # https://github.com/actions/checkout/releases (v6.0.0, 2025-11-11)
  #       uses: actions/checkout@v6.0.0
  #       with:
  #         sparse-checkout-cone-mode: false  # Disabled for cross-platform compatibility between GitHub Actions and act local runner

  #     - name: Begin Workflow Job
  #       id: begin-workflow-job
  #       uses: ./.github/actions/workflow-job-begin

  #     - name: Set up Go and validate dependencies
  #       uses: ./.github/actions/go-setup
  #       with:
  #         go-version: ${{ steps.begin-workflow-job.outputs.go-version }}
  #         go-mod-download: true
  #         go-mod-verify: false
  #         go-mod-tidy: false

  #     - name: Copy requirements coverage file to expected location
  #       run: |
  #         echo "📋 Preparing requirements coverage file..."
  #         mkdir -p docs/02-identityV2
  #         cp docs/02-identityV2/passthru4/REQUIREMENTS-COVERAGE.md docs/02-identityV2/REQUIREMENTS-COVERAGE.md

  #     - name: Run identity requirements coverage check (strict mode)
  #       run: |
  #         echo "📋 Checking requirements coverage with strict thresholds..."
  #         START_TIME=$(date +%s)
  #         echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

  #         # Run with --skip-slow-checks to avoid task-specific parsing (not implemented in coverage file)
  #         go run ./cmd/cicd go-identity-requirements-check --strict \
  #           --task-threshold=90 \
  #           --overall-threshold=85 \
  #           --skip-slow-checks

  #         END_TIME=$(date +%s)
  #         DURATION=$((END_TIME - START_TIME))
  #         echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
  #         echo "⏱️ Requirements check completed in: ${DURATION}s"

  #     - name: Upload requirements coverage report
  #       if: always()
  #       # https://github.com/actions/upload-artifact/releases (v5.0.0, 2025-04-21)
  #       uses: actions/upload-artifact@v5.0.0
  #       with:
  #         name: identity-requirements-coverage
  #         path: |
  #           docs/02-identityV2/REQUIREMENTS-COVERAGE.md
  #         retention-days: 30

  #     - name: Identity requirements summary
  #       if: always()
  #       uses: ./.github/actions/workflow-job-end
