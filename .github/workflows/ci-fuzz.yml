# $schema: https://json.schemastore.org/github-workflow.json
# https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax

name: CI - Fuzz Testing
# CROSS-PLATFORM COMPATIBILITY: All file references in this workflow MUST use relative paths
# to support both GitHub Actions Ubuntu runners and Windows `act` local runner testing.
# Absolute paths (e.g., C:\...) break cross-platform compatibility.
# Example: Use './deployments/compose/compose.yml'

on:
  push:
    branches: [ main, develop ]
    # SHARED PATHS-IGNORE CONFIGURATION - Keep synchronized with pull_request below
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
      - '.github/copilot-instructions.md'
      - '.github/instructions/**'
      - 'workflow-reports/**'
      - 'nohup.out'
      - 'LICENSE'
      - '.editorconfig'
      - '.gitignore'
      - '.gitattributes'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/pull_request_template.md'
      - '.github/dependabot.yml'
      - '**/*.log'
      - '**/*.sarif'
  pull_request:
    branches: [ main, develop ]
    # SHARED PATHS-IGNORE CONFIGURATION - Keep synchronized with push above
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
      - '.github/copilot-instructions.md'
      - '.github/instructions/**'
      - 'workflow-reports/**'
      - 'nohup.out'
      - 'LICENSE'
      - '.editorconfig'
      - '.gitignore'
      - '.gitattributes'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/pull_request_template.md'
      - '.github/dependabot.yml'
      - '**/*.log'
      - '**/*.sarif'
  workflow_dispatch:
    inputs:
      test_profile:
        description: 'Test profile to run'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - fuzz
          - fuzz-keygen
          - fuzz-digests

env:
  GO_VERSION: '1.25.3'

jobs:
  # Fuzz Testing - Keygen Package - Property-based testing for cryptographic key generation
  fuzz-keygen:
    name: Fuzz Tests - Keygen
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.test_profile == 'all' || github.event.inputs.test_profile == 'fuzz' || github.event_name != 'workflow_dispatch' }}

    steps:
    - name: Workflow Start - CI - Fuzz Testing / fuzz-keygen (ci-fuzz.yml)
      run: |
        echo "=========================================="
        echo "Workflow: ${{ github.workflow }}"
        echo "File: .github/workflows/ci-fuzz.yml"
        echo "Job: fuzz-keygen"
        echo "Triggered by: ${{ github.event_name }}"
        echo "Start Time: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "=========================================="

    - name: Checkout code
      uses: actions/checkout@v5.0.0
      with:
        sparse-checkout-cone-mode: false  # Disabled for cross-platform compatibility between GitHub Actions and act local runner

    - name: Set up Go and validate dependencies
      uses: ./.github/actions/go-setup
      with:
        go-version: ${{ env.GO_VERSION }}
        go-mod-download: true
        go-mod-verify: true
        go-mod-tidy: true

    - name: RSA Keygen Fuzz Test
      run: |
        echo "� Running RSA keygen fuzz test..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        go test -fuzz=FuzzGenerateRSAKeyPair -fuzztime=30s -timeout=60s ./internal/common/crypto/keygen
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ RSA keygen fuzz test completed in: ${DURATION}s"

    - name: ECDSA Keygen Fuzz Test
      run: |
        echo "🔐 Running ECDSA keygen fuzz test..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        go test -fuzz=FuzzGenerateECDSAKeyPair -fuzztime=15s -timeout=30s ./internal/common/crypto/keygen
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ ECDSA keygen fuzz test completed in: ${DURATION}s"

    - name: ECDH Keygen Fuzz Test
      run: |
        echo "🔐 Running ECDH keygen fuzz test..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        go test -fuzz=FuzzGenerateECDHKeyPair -fuzztime=15s -timeout=30s ./internal/common/crypto/keygen
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ ECDH keygen fuzz test completed in: ${DURATION}s"

    - name: EdDSA Keygen Fuzz Test
      run: |
        echo "🔐 Running EdDSA keygen fuzz test..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        go test -fuzz=FuzzGenerateEDDSAKeyPair -fuzztime=15s -timeout=30s ./internal/common/crypto/keygen
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ EdDSA keygen fuzz test completed in: ${DURATION}s"

    - name: AES Keygen Fuzz Test
      run: |
        echo "🔐 Running AES keygen fuzz test..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        go test -fuzz=FuzzGenerateAESKey -fuzztime=15s -timeout=30s ./internal/common/crypto/keygen
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ AES keygen fuzz test completed in: ${DURATION}s"

    - name: AES-HS Keygen Fuzz Test
      run: |
        echo "🔐 Running AES-HS keygen fuzz test..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        go test -fuzz=FuzzGenerateAESHSKey -fuzztime=15s -timeout=30s ./internal/common/crypto/keygen
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ AES-HS keygen fuzz test completed in: ${DURATION}s"

    - name: HMAC Keygen Fuzz Test
      run: |
        echo "🔐 Running HMAC keygen fuzz test..."
        START_TIME=$(date +%s)
        echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        go test -fuzz=FuzzGenerateHMACKey -fuzztime=15s -timeout=30s ./internal/common/crypto/keygen
        END_TIME=$(date +%s)
        DURATION=$((END_TIME - START_TIME))
        echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        echo "⏱️ HMAC keygen fuzz test completed in: ${DURATION}s"

    - name: Keygen fuzz test summary
      run: |
          echo "## 🔐 Keygen Fuzz Testing Results" >> $GITHUB_STEP_SUMMARY
          echo "Fuzz tests for cryptographic key generation completed successfully." >> $GITHUB_STEP_SUMMARY
          echo "- Coverage: 7 key generation functions" >> $GITHUB_STEP_SUMMARY
          echo "- Duration: RSA=30s, Others=15s each (parallel steps)" >> $GITHUB_STEP_SUMMARY
          echo "- Purpose: Property-based testing for secure key generation" >> $GITHUB_STEP_SUMMARY

  # Fuzz Testing - Digests Package - Property-based testing for cryptographic hash functions
  fuzz-digests:
    name: Fuzz Tests - Digests
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.test_profile == 'all' || github.event.inputs.test_profile == 'fuzz' || github.event_name != 'workflow_dispatch' }}

    steps:
      - name: Workflow Start - CI - Fuzz Testing / fuzz-digests (ci-fuzz.yml)
        run: |
          echo "=========================================="
          echo "Workflow: ${{ github.workflow }}"
          echo "File: .github/workflows/ci-fuzz.yml"
          echo "Job: fuzz-digests"
          echo "Triggered by: ${{ github.event_name }}"
          echo "Start Time: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "=========================================="

      - name: Checkout code
        uses: actions/checkout@v5.0.0
        with:
          sparse-checkout-cone-mode: false  # Disabled for cross-platform compatibility between GitHub Actions and act local runner

      - name: Set up Go and validate dependencies
        uses: ./.github/actions/go-setup
        with:
          go-version: ${{ env.GO_VERSION }}
          go-mod-download: true
          go-mod-verify: true
          go-mod-tidy: true

      - name: HKDF All Variants Fuzz Test
        run: |
          echo "🔐 Running HKDF All Variants fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzHKDFAllVariants -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ HKDF All Variants fuzz test completed in: ${DURATION}s"

      - name: HKDF SHA256 Fuzz Test
        run: |
          echo "🔐 Running HKDF-SHA256 fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzHKDFwithSHA256 -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ HKDF-SHA256 fuzz test completed in: ${DURATION}s"

      - name: HKDF SHA384 Fuzz Test
        run: |
          echo "🔐 Running HKDF-SHA384 fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzHKDFwithSHA384 -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ HKDF-SHA384 fuzz test completed in: ${DURATION}s"

      - name: HKDF SHA512 Fuzz Test
        run: |
          echo "🔐 Running HKDF-SHA512 fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzHKDFwithSHA512 -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ HKDF-SHA512 fuzz test completed in: ${DURATION}s"

      - name: HKDF SHA224 Fuzz Test
        run: |
          echo "🔐 Running HKDF-SHA224 fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzHKDFwithSHA224 -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ HKDF-SHA224 fuzz test completed in: ${DURATION}s"

      - name: SHA512 Fuzz Test
        run: |
          echo "🔐 Running SHA512 fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzSHA512 -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ SHA512 fuzz test completed in: ${DURATION}s"

      - name: SHA384 Fuzz Test
        run: |
          echo "🔐 Running SHA384 fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzSHA384 -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ SHA384 fuzz test completed in: ${DURATION}s"

      - name: SHA256 Fuzz Test
        run: |
          echo "🔐 Running SHA256 fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzSHA256 -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ SHA256 fuzz test completed in: ${DURATION}s"

      - name: SHA224 Fuzz Test
        run: |
          echo "🔐 Running SHA224 fuzz test..."
          START_TIME=$(date +%s)
          echo "📅 Start: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          go test -fuzz=FuzzSHA224 -fuzztime=15s -timeout=30s ./internal/common/crypto/digests
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "📅 End: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "⏱️ SHA224 fuzz test completed in: ${DURATION}s"

      - name: Digests fuzz test summary
        run: |
          echo "## 🔐 Digests Fuzz Testing Results" >> $GITHUB_STEP_SUMMARY
          echo "Fuzz tests for cryptographic digest functions completed successfully." >> $GITHUB_STEP_SUMMARY
          echo "- Coverage: 9 digest functions (HKDF variants + SHA-2 family)" >> $GITHUB_STEP_SUMMARY
          echo "- Duration: 15s each (parallel steps)" >> $GITHUB_STEP_SUMMARY
          echo "- Purpose: Property-based testing for secure hash operations" >> $GITHUB_STEP_SUMMARY
