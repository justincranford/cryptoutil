<?xml version="1.0" encoding="UTF-8"?>
<ruleset comparisonMethod="maven"
         xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0 http://mojo.codehaus.org/versions-maven-plugin/xsd/rule-2.0.0.xsd">

  <!-- Allow patch updates for stable dependencies -->
  <rules>
    <rule groupId="*" artifactId="*" comparisonMethod="maven">
      <allowMajorUpdates>false</allowMajorUpdates>
      <allowMinorUpdates>true</allowMinorUpdates>
      <allowIncrementalUpdates>true</allowIncrementalUpdates>
    </rule>
  </rules>

  <!-- Ignore specific dependencies that should not be updated -->
  <ignoreVersions>
    <!-- Gatling versions are tightly coupled -->
    <ignoreVersion type="regex">.*gatling.*</ignoreVersion>
    <!-- Test dependencies can be updated more aggressively -->
    <ignoreVersion type="regex">.*junit.*</ignoreVersion>
    <ignoreVersion type="regex">.*testng.*</ignoreVersion>
    <ignoreVersion type="regex">.*mockito.*</ignoreVersion>
  </ignoreVersions>

</ruleset>
