# Implementation Plan - Framework V12: TLS Wiring

**Status**: Planning
**Created**: 2025-06-26
**Last Updated**: 2026-04-16
**Purpose**: Wire PostgreSQL TLS/mTLS (pki-init certs from v11) into shared-postgres and PS-ID deployment configs; configure Private PS-ID App admin endpoint mTLS. OTel Collector mTLS, Grafana LGTM mTLS, and Public PS-ID App TLS trust are split to Framework V13.

## Quality Mandate - MANDATORY

**Quality Attributes (NO EXCEPTIONS)**:
- ✅ **Correctness**: ALL code must be functionally correct with comprehensive tests
- ✅ **Completeness**: NO phases or tasks or steps skipped, NO features de-prioritized, NO shortcuts
- ✅ **Thoroughness**: Evidence-based validation at every step
- ✅ **Reliability**: Quality gates enforced (≥95%/98% coverage/mutation)
- ✅ **Efficiency**: Optimized for maintainability and performance, NOT implementation speed
- ✅ **Accuracy**: Changes must address root cause, not just symptoms
- ❌ **Time Pressure**: NEVER rush, NEVER skip validation, NEVER defer quality checks
- ❌ **Premature Completion**: NEVER mark phases or tasks or steps complete without objective evidence

**ALL issues are blockers - NO exceptions.**

## Overview

Framework V12 takes the cert directory structure generated by v11's pki-init and wires it into PostgreSQL and private admin endpoint configurations. This includes:

1. **PostgreSQL mTLS**: Leader/follower server certs, client certs for app instances, replication mTLS, HBA rules, GORM connection strings.
2. **Private PS-ID App mTLS Trust**: Admin (:9090) endpoint server cert and client CA trust for all app instance variants.

OTel Collector mTLS, Grafana LGTM mTLS, and Public PS-ID App TLS trust are deferred to **Framework V13**.

**Key Constraint**: `./configs/` continues to use TLS auto-generate mode — no pki-init dependency. Only `./deployments/` leverages static certs generated by pki-init, mounted from the `__PS_ID__-certs` named Docker volume.

**Principle of Least Privilege (CRITICAL)**: Every service MUST mount ONLY the specific cert directories it needs. No broad `/certs` volume mounts. This is enforced by deployment templates.

## Background

Framework V11 (prerequisite) redesigned the `/certs` volume layout with 14 categories, keystore/truststore directories, PKCS#12 + PEM file format, and the `SAME-AS-DIR-NAME` file naming convention. V11 also resolved Docker volume strategy (named volumes with least-privilege permissions).

V12 was originally scoped to wire ALL TLS (PostgreSQL, OTel, Grafana, App). After planning, the scope was split:
- **V12** (this plan): PostgreSQL mTLS + Private PS-ID App mTLS Trust
- **V13**: OTel Collector mTLS + Grafana LGTM mTLS + Public PS-ID App TLS Trust

Phase 0 of V12 patches pki-init for structural changes needed by both V12 (Cat 14 postgres-only) and V13 (Cat 9 infra entity). V13 assumes V12 Phase 0 is complete.

Decisions inherited from v11 quizme / planning: D2 (GORM SSL config), D3 (Cat 9 infra entity — implemented in Phase 0, consumed in V13), D4 (Cat 14 postgres-only), D5 (named volume strategy). D1 (Grafana HTTPS) and D6 (Grafana mTLS) are fully deferred to V13.

## Technical Context

- **Language**: Go 1.26.1
- **Cert Structure**: `docs/tls-structure.md` (14 categories, keystore/truststore pattern)
- **PostgreSQL**: shared-postgres compose with leader + follower
- **Framework Config**: `internal/apps/framework/service/config/` — YAML field additions for SSL params
- **Dependencies**: V11 must be complete before V12 implementation begins; V12 Phase 0 must be complete before V13 begins

## Phases

### Phase 0: pki-init Patch — Cat 9 infra + Cat 14 postgres-only (3h) [Status: ☐ TODO]

**Objective**: Apply D3 and D4 structural changes to the pki-init generator. Prerequisites for both V12 (Cat 14 postgres-only for PostgreSQL client certs) and V13 (Cat 9 infra entity for OTel→Grafana).
- Add `PKIInitEntityInfra = "infra"` magic constant to `internal/shared/magic/magic_pkiinit.go`.
- Add `PKIInitPostgresAppInstanceSuffixes()` function to `tier.go` returning `["postgres-1", "postgres-2"]`.
- Cat 9 `generateSharedCAs()`: add `infra` cert generation after `admin` (2 new dirs: `grafana-otel-lgtm-https-client-entity-infra/` and `otel-collector-contrib-https-client-entity-infra/`).
- Cat 14 `generatePSIDCerts()`: replace `PKIInitAppInstanceSuffixes()` with `PKIInitPostgresAppInstanceSuffixes()` in Cat 14 loop. Update comment from "8 dirs" to "4 dirs".
- Update `generateSharedCAs` comment: change `9 (admin only)` to `9 (admin+infra)`.
- Update generator unit tests for new expected dir counts (recalculate in code).
- Verify: `go test ./internal/apps/framework/tls/... -run TestGenerate`.
- **Success**: pki-init generates correct dirs; Cat 9 includes `infra`, Cat 14 has 4 postgres-only dirs; all tests pass.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 1: PostgreSQL Server TLS — Leader + Follower (5h) [Status: ☐ TODO]

**Objective**: Configure PostgreSQL leader and follower to serve TLS connections using pki-init certs mounted from `/certs` volume.
- Leader `postgresql.conf`: `ssl = on`, `ssl_cert_file`, `ssl_key_file`, `ssl_min_protocol_version = TLSv1.3`.
- Follower `postgresql.conf`: same settings with follower-specific cert paths.
- `ssl_ca_file` = Cat 12 `postgres-tls-client-issuing-ca/truststore/` on both (for future client cert verification).
- `pg_hba.conf`: allow `hostssl` connections (no `clientcert=verify-full` yet — added in Phase 4).
- Update init scripts: set correct file permissions on mounted cert files.
- **Cert mounts — principle of least privilege**:
  - Leader: Cat 11 `postgres-tls-server-entity-leader/` keystore + Cat 12 `postgres-tls-client-issuing-ca/truststore/`
  - Follower: Cat 11 `postgres-tls-server-entity-follower/` keystore + Cat 12 `postgres-tls-client-issuing-ca/truststore/`
- Named volume: `__PS_ID__-certs` defined in PS-ID compose, referenced (not re-declared) in shared-postgres compose (D5: include-merged approach).
- **Success**: `psql "sslmode=require"` connects to leader and follower; TLS 1.3 negotiated; plain connections still accepted (not yet rejected).
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 2: PostgreSQL Replication Server TLS (2h) [Status: ☐ TODO]

**Objective**: Update follower→leader replication to use server TLS (server cert verification only, no client cert yet).
- Follower `primary_conninfo`: add `sslmode=verify-full` and `sslrootcert=<Cat 10 truststore path>`.
- Leader `pg_hba.conf` replication entry: `hostssl replication ... scram-sha-256` (no `clientcert` yet).
- **Additional cert mount for follower** (least privilege): Cat 10 `postgres-tls-server-issuing-ca/truststore/` for verifying the leader's server cert.
- Verify replication slot reconnects over TLS.
- **Success**: `pg_stat_replication` on leader shows `ssl = t`; replication lag returns to zero.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 3: Verify PostgreSQL Standalone (2h) [Status: ☐ TODO]

**Objective**: Confirm each PostgreSQL node responds to TLS connections independently before adding client cert requirements.
- Leader: `psql "sslmode=verify-full sslrootcert=<Cat 10>"` connects; `SELECT ssl_version, ssl_cipher FROM pg_stat_ssl WHERE pid = pg_backend_pid()` returns TLSv1.3.
- Follower: same verification.
- Replication: `SELECT * FROM pg_stat_replication` on leader shows active slot with `ssl = t`.
- Confirm `sslmode=disable` connections still accepted (will be rejected in Phase 4).
- **Success**: Both nodes load certs correctly; replication runs over TLS; no regressions from plain-text access.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 4: PostgreSQL Client mTLS — HBA + GORM Config (6h) [Status: ☐ TODO]

**Objective**: Require client certs for app→PostgreSQL connections and configure GORM to present them.
- Update `pg_hba.conf`: all `hostssl` app connection rules change to `clientcert=verify-full`; plain `host` rules removed.
- Add YAML fields to framework config: `database.sslmode`, `database.sslcert`, `database.sslkey`, `database.sslrootcert` (D2: YAML config approach).
- GORM DSN builder: append `sslmode`, `sslcert`, `sslkey`, `sslrootcert` params when `sslmode = verify-full`.
- **Cert mounts — principle of least privilege**:
  - postgres-1 instance: Cat 14 `postgres-tls-client-entity-leader-{PS-ID}-postgres-1/` keystore + Cat 10 `postgres-tls-server-issuing-ca/truststore/`
  - postgres-2 instance: Cat 14 `postgres-tls-client-entity-leader-{PS-ID}-postgres-2/` keystore + Cat 10 `postgres-tls-server-issuing-ca/truststore/`
  - sqlite-1, sqlite-2 instances: NO PostgreSQL cert dirs mounted (they use SQLite, never connect to PG)
- **Success**: postgres-1 and postgres-2 instances connect via mTLS; non-client-cert connections rejected by PG HBA.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 5: PostgreSQL Replication Client mTLS (2h) [Status: ☐ TODO]

**Objective**: Add client cert to follower's replication connection and require it on the leader.
- Follower `primary_conninfo`: add `sslcert=<Cat 13 follower-replication keystore crt>` and `sslkey=<Cat 13 follower-replication keystore key>`.
- Leader `pg_hba.conf` replication entry: update to `hostssl replication ... scram-sha-256 clientcert=verify-full`.
- **Additional cert mount for follower** (least privilege): Cat 13 `postgres-tls-client-entity-follower-replication/` keystore.
- Verify replication reconnects and presents the client cert.
- **Success**: `pg_stat_replication` shows `ssl=t`; replication without client cert is rejected.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 6: Verify PostgreSQL Full Stack (2h) [Status: ☐ TODO]

**Objective**: End-to-end verification of full mTLS with active replication.
- Verify postgres-1 and postgres-2 app instances connect to leader via mTLS.
- Verify non-mTLS and wrong-cert connections are rejected by leader and follower.
- Write data to leader; verify it replicates to follower.
- `pg_stat_ssl` confirms TLS for all active connections.
- **Success**: Full mTLS working; replication verified; data integrity confirmed; no plain-text connections accepted.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 7: Deployment Templates for PostgreSQL TLS (4h) [Status: ☐ TODO]

**Objective**: Update canonical deployment templates to wire PG TLS cert mounts and SSL connection params.
- Update `deployment-templates.md`: add cert volume mount rules for PG TLS (least privilege table: leader, follower, postgres-1 app, postgres-2 app; sqlite-1/2 explicitly excluded).
- Update `api/cryptosuite-registry/templates/` shared-postgres compose template: per-node cert mounts (Cat 11 + Cat 12 for leader; Cat 10 + Cat 11 + Cat 12 + Cat 13 for follower).
- Update PS-ID compose template for postgres-1 and postgres-2 app instances: Cat 14 + Cat 10 cert dirs only.
- Update `postgres-url.secret` template value: `sslmode=verify-full&sslrootcert=...&sslcert=...&sslkey=...`.
- Update `__PS_ID__-app-framework-postgresql-1.yml` and `-postgresql-2.yml` templates with cert config paths.
- Verify sqlite-1 and sqlite-2 templates explicitly omit all PG cert dirs.
- **Success**: All template files updated; cert mount rules documented; least privilege enforced per service.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 8: Deployment Linting (1h) [Status: ☐ TODO]

**Objective**: Ensure all updated deployment files pass lint-deployments.
- Run: `go run ./cmd/cicd-lint lint-deployments`
- Fix any violations found by the 8 validators.
- Run: `golangci-lint run ./internal/apps/tools/cicd_lint/lint_deployments/...`
- **Success**: `lint-deployments` exits 0; all 8 validators pass; no violations.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 9: Deployment Verification — PostgreSQL TLS (3h) [Status: ☐ TODO]

**Objective**: Start deployments and verify PG TLS connectivity and replication end-to-end.
- `docker compose up` with pki-init + shared-postgres + one PS-ID service (postgres-1 and postgres-2 instances).
- Verify postgres-1 app instance connects to PG leader via mTLS.
- Verify PG leader replicates to follower: `pg_stat_replication` shows active slot.
- Verify data written to leader via postgres-1 app is readable from follower.
- Verify deployment health checks pass.
- **Success**: Full PG TLS deployment verified; connectivity and replication confirmed end-to-end.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 10: Private PS-ID App mTLS Trust (4h) [Status: ☐ TODO]

**Objective**: Configure all PS-ID app instances to serve the private admin (:9090) endpoint with mTLS, using pki-init certs.
- Framework server builder: load admin server cert from `private-https-mutual-entity-{PS-ID}-{sqlite,postgres}-{1,2}/` (Cat 7 keystore). This cert has both Server Auth and Client Auth EKU.
- Framework server builder: load admin CA truststore from `private-https-mutual-issuing-ca-{PS-ID}-{sqlite,postgres}-{1,2}/truststore/` (Cat 6) for verifying admin clients.
- **Cert mounts — principle of least privilege** (per app instance variant):
  - sqlite-1: Cat 7 `private-https-mutual-entity-{PS-ID}-sqlite-1/` + Cat 6 `private-https-mutual-issuing-ca-{PS-ID}-sqlite-1/truststore/`
  - sqlite-2: Cat 7 `private-https-mutual-entity-{PS-ID}-sqlite-2/` + Cat 6 `private-https-mutual-issuing-ca-{PS-ID}-sqlite-2/truststore/`
  - postgres-1: Cat 7 `private-https-mutual-entity-{PS-ID}-postgres-1/` + Cat 6 `private-https-mutual-issuing-ca-{PS-ID}-postgres-1/truststore/`
  - postgres-2: Cat 7 `private-https-mutual-entity-{PS-ID}-postgres-2/` + Cat 6 `private-https-mutual-issuing-ca-{PS-ID}-postgres-2/truststore/`
  - NO public cert dirs, NO PG cert dirs mounted for admin — only Cat 6 + Cat 7
- Update deployment config templates: `server.admin-cert-file`, `server.admin-key-file`, `server.admin-ca-file` YAML fields.
- Update deployment compose templates: add Cat 6 + Cat 7 cert mounts per instance variant.
- Run lint-deployments and verify.
- Verify admin endpoint `/admin/api/v1/livez` responds over mTLS.
- **Success**: All 4 instance variants serve admin mTLS; wrong-cert admin connections rejected; lint passes.
- **Post-Mortem**: After quality gates pass, update lessons.md.

### Phase 11: Knowledge Propagation (2h) [Status: ☐ TODO]

**Objective**: Apply lessons learned to permanent artifacts — NEVER skip this phase.
- Review lessons.md from all prior phases.
- Update ENG-HANDBOOK.md: PostgreSQL mTLS patterns, GORM SSL config, private admin mTLS, cert mount least privilege rules.
- ENG-HANDBOOK.md §10.3.4 `InsecureSkipVerify` fix (deferred from framework-v11 per Q7=E): Update §10.3.4 test example to use `RootCAs: testCAPool`; remove `InsecureSkipVerify: true` from the test example.
- Update `deployment-templates.md` with cert mount rules and the least privilege table.
- Verify propagation integrity: `go run ./cmd/cicd-lint lint-docs`.
- **Success**: All artifact updates committed; propagation check passes.

## Executive Decisions

### Decision D2: GORM PostgreSQL Client Cert Configuration Method (Q2=A)

**Options**:
- A: YAML config file with explicit `database.sslcert`, `database.sslkey`, `database.sslrootcert` fields ✓ **SELECTED**
- B: DSN query parameters (`sslcert=...&sslkey=...&sslrootcert=...` appended to connection string)
- C: GORM `Config.ConnPool` with custom `*tls.Config` injected at startup

**Decision**: Option A — YAML config fields loaded by the framework config parser.

**Rationale**: Consistent with existing config-file-first pattern (Docker secrets > YAML > CLI). DSN query parameters (B) expose cert paths in the connection string which complicates secrets management. Custom ConnPool (C) requires code changes across all services.

**Impact on Phase 2**: Task 2.2 adds `database.sslcert`, `database.sslkey`, `database.sslrootcert` to framework config YAML and uses them when building the GORM DSN.

**YAML snippet**:
```yaml
database:
  sslmode: verify-full
  sslcert: /certs/__PS_ID__/postgres-tls-client-entity-leader-__PS_ID__-postgres-1/postgres-tls-client-entity-leader-__PS_ID__-postgres-1.crt
  sslkey: /certs/__PS_ID__/postgres-tls-client-entity-leader-__PS_ID__-postgres-1/postgres-tls-client-entity-leader-__PS_ID__-postgres-1.key
  sslrootcert: /certs/__PS_ID__/postgres-tls-server-issuing-ca/truststore/postgres-tls-server-issuing-ca.crt
```

---

### Decision D3: Cat 9 OTel→Grafana Service-to-Service Client Cert (Q1=E)

**Options**:
- A: Reuse admin cert (`grafana-otel-lgtm-https-client-entity-admin/`)
- B: Name it after the connecting service (`grafana-otel-lgtm-https-client-entity-otel-collector-contrib/`)
- C: Server-TLS-only (no client cert), OTel Collector does not present a cert
- D: Move to Cat 13 as a replication-style peer cert
- E: New `infra` entity type in Cat 9 (alongside `admin`) ✓ **SELECTED**

**Decision**: Add a new `infra` entity type to Cat 9 for service-to-service forwarding certs. Generates `grafana-otel-lgtm-https-client-entity-infra/` and `otel-collector-contrib-https-client-entity-infra/`.

**Rationale**: `admin` certs are for human operators. OTel Collector acting as a forwarding agent is an infrastructure concern, not an admin concern. A dedicated `infra` entity type cleanly separates human vs. machine access. Naming it after the connecting service (B) would be confusing since both grafana and otel get infra certs. The `infra` label is generalizable to future service-to-service connections.

**Impact on Phase 0**: pki-init generator must add infra cert generation to `generateSharedCAs()` (2 new dirs). Cat 9 count increases from 10→12 per PS-ID, 82→84 per SUITE. tls-structure.md updated.

**Impact on Framework V13**: OTel Collector config (V13 Phase 1+2) uses `otel-collector-contrib-https-client-entity-infra/` cert; Grafana OTLP ingest (V13 Phase 4+5) uses `grafana-otel-lgtm-https-client-entity-infra/` cert for the OTel→Grafana forwarding pipeline.

---

### Decision D4: Cat 14 PostgreSQL App Client Cert Scope (Q2=E)

**Options**:
- A: Generate certs for all 4 instances (sqlite-1, sqlite-2, postgres-1, postgres-2)
- B: Generate certs only for postgres-1/postgres-2; generate empty placeholder dirs for sqlite-1/sqlite-2
- C: Remove Cat 14 entirely; use a single shared app-client cert
- D: Separate Cat 14 into two sub-categories: sqlite (server-TLS-only trust) and postgres (mTLS)
- E: Remove sqlite variants from Cat 14; only generate postgres-1 and postgres-2 ✓ **SELECTED**

**Decision**: Cat 14 generates certs only for `postgres-1` and `postgres-2` app instances. `sqlite-1` and `sqlite-2` instances never connect to PostgreSQL and need no PostgreSQL client certs.

**Rationale**: SQLite app instances use in-memory SQLite, not PostgreSQL. Generating PostgreSQL client certs for them is incorrect and wasteful. Reducing from 8 to 4 dirs per PS-ID simplifies the structure and removes unused artifacts.

**Impact on Phase 0**: pki-init generator Cat 14 loop uses `PKIInitPostgresAppInstanceSuffixes()` (new function returning `["postgres-1", "postgres-2"]`) instead of `PKIInitAppInstanceSuffixes()`. Cat 14 count decreases from 8→4 per PS-ID, 80→40 per SUITE. Total per PS-ID: 92→90. tls-structure.md updated.

---

### Decision D5: Cross-Compose Named Volume Strategy (Q3=A)

**Options**:
- A: Include-merged approach — each PS-ID compose file defines the named volume; shared-postgres/shared-telemetry composes reference it without declaring it (volume merges from PS-ID scope) ✓ **SELECTED**
- B: Declare a global suite-level named volume `cryptoutil-certs` in a top-level compose; all PS-ID and shared composes reference it as `external: true`
- C: Bind mount: use `./certs/__PS_ID__/` as a bind mount instead of a named volume
- D: Copy certs into shared containers via init containers; no shared volume

**Decision**: Include-merged approach. PS-ID compose defines `__PS_ID__-certs` named volume. Docker Compose include-merge makes it visible to all included shared composes.

**Rationale**: Named volumes defined in PS-ID compose files are available in the merged project scope. shared-postgres and shared-telemetry reference them without re-declaring. This is standard Docker Compose behavior confirmed by Docker documentation.

**Impact on Phases 1, 3**: PS-ID composes define `__PS_ID__-certs` volume; shared-postgres and shared-telemetry composes reference it by name without `external: true`. No global suite-level volume needed.

---

### Decision D6: grafana/otel-lgtm mTLS on OTLP Ingest (Q4=E)

**Options**:
- A: Assume mTLS is supported; proceed with full mTLS implementation
- B: Test first: add integration test that checks mTLS handshake on :14317/:14318 before wiring
- C: Implement server-TLS-only (no client cert on OTLP ingest); full mTLS only for Grafana UI
- D: Replace grafana/otel-lgtm with separate grafana + otel-collector-contrib containers for independent TLS control
- E: Assume mTLS is supported (grafana/otel-lgtm embeds OTel Collector Contrib); NEVER replace with separate containers ✓ **SELECTED**

**Decision**: Assume grafana/otel-lgtm supports mTLS on OTLP ingest. Proceed with full mTLS. NEVER replace with separate containers.

**Rationale**: grafana/otel-lgtm embeds the official OTel Collector Contrib distribution, which supports mTLS on OTLP endpoints out of the box via `tls.client_ca_file`, `tls.cert_file`, `tls.key_file`. The image architecture is a combined deployment for development convenience; breaking it apart would increase operational complexity without benefit.

**Impact on Phase 5**: Configure OTel Collector’s exporter with full mTLS to Grafana LGTM OTLP ingest, using the `infra` cert from D3. If mTLS fails at runtime, fallback is server-TLS-only (not separate containers).

---

## Risk Assessment

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|------------|
| PostgreSQL HBA `clientcert=verify-full` locks out apps before GORM cert config is live | High | High | Phase 4 order: update GORM config + verify first, then tighten HBA |
| Cert file permissions on mounted volume (PostgreSQL rejects world-readable key) | High | High | Init script sets `chmod 600` on key files; add to Phase 1 acceptance criteria |
| Named volume not visible to shared-postgres after include-merge | Medium | High | Test D5 volume merge in Phase 1 before adding TLS config complexity |
| Follower replication slot drops during TLS reconfiguration | Medium | Medium | Monitor `pg_stat_replication`; restart follower if slot is invalidated |
| Performance impact of mTLS on replication throughput | Low | Low | Benchmark before/after in Phase 6 |

## Quality Gates - MANDATORY

Same as v11 — see `docs/framework-v11/plan.md` Quality Gates section.

## Success Criteria

- [ ] All 12 phases complete (Phase 0 through Phase 11)
- [ ] PostgreSQL mTLS verified: app instances connect via mTLS; replication uses mTLS; non-mTLS rejected
- [ ] Private PS-ID App admin endpoint verified: all 4 variants serve mTLS; wrong-cert connections rejected
- [ ] Deployment templates updated and lint-deployments passes
- [ ] Principle of least privilege enforced in all templates: each service mounts only required cert dirs
- [ ] All quality gates passing
- [ ] `./configs/` unchanged (auto-TLS mode only)
- [ ] Documentation updated (ENG-HANDBOOK.md, deployment-templates.md)
- [ ] CI/CD workflows green

## ENG-HANDBOOK.md Cross-References - MANDATORY

| Topic | Section | Relevance |
|-------|---------|-----------|
| Testing Strategy | [Section 10](../../docs/ENG-HANDBOOK.md#10-testing-architecture) | E2E testing patterns |
| E2E Testing | [Section 10.4](../../docs/ENG-HANDBOOK.md#104-e2e-testing-strategy) | Docker Compose E2E |
| Quality Gates | [Section 11.2](../../docs/ENG-HANDBOOK.md#112-quality-gates) | Coverage and mutation targets |
| Deployment Architecture | [Section 12](../../docs/ENG-HANDBOOK.md#12-deployment-architecture) | Docker Compose patterns |
| Security Architecture | [Section 6](../../docs/ENG-HANDBOOK.md#6-security-architecture) | TLS and mTLS patterns |
| OTel/Telemetry | [Section 9.4](../../docs/ENG-HANDBOOK.md#94-telemetry-strategy) | OTel configuration |
| Plan Lifecycle | [Section 14.6](../../docs/ENG-HANDBOOK.md#146-plan-lifecycle-management) | Plan management |
| Post-Mortem | [Section 14.8](../../docs/ENG-HANDBOOK.md#148-phase-post-mortem--knowledge-propagation) | Phase lessons |
